import {destinations} from '../sharedData/destinationsdata';
import * as ActionTypes from './ActionTypes';


export const cities = (state = destinations, action) => {
	switch (action.type) {
		default:
			return state;
	}
}
